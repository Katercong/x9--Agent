# 进行中任务（WIP）

> 当前会话留下的"做到一半"清单。新会话接手时直接看这里就知道下一步该干啥。
> 完成一项划掉一项，新增任务追加到底部。

---

## 🔥 当前正在做：TK 热搜抓取交张负责后的 UI 改造

**背景**：原本 2.2.2 由廖做，现完全由张负责。基础设施全在，但适合廖（爬虫开发者）的入口对张（BD/管理员）不友好。

### 已完成
- ✅ 数据层（migrate_v10）：keyword_snapshot / scrape_run / 启发式分类 trigger
- ✅ AI 分析层（app/keyword_ai.py）：enrich + dashboard + trend 端点
- ✅ 廖用的 scrape 模板（scripts/scrape_tk_hot.py）— **保留作为可选** C/D 路线时用
- ✅ 前台 🔥 TK 热搜 tab（实时仪表盘 + 30s 轮询）
- ✅ 4 条路线对齐 + 推荐路径（A+B 立刻能用，C 长期）

### 下一步要建（按优先级）
1. **"+ 手动添加关键词"按钮** 在 🔥 TK 热搜 tab → 弹出表单 → 张能直接录入
2. **"🧠 AI 推荐关键词"按钮** + `POST /api/v1/ai/keywords/suggest` 端点
   - 输入：品类 + 数量 + 备注
   - 输出：LLM 头脑风暴 N 个建议词，张选择性写入
3. **`docs/TK热搜数据来源选择.md`** — 写明 4 条路线（A/B/C/D）+ 张如何选
4. （可选）**Step 2** 接 Pentos / Tikbuddy 第三方 API（等张确认要不要花钱）

### 关键文件
- 数据：`scripts/migrate_v10.py` / `app/keyword_ai.py`
- 前端：`app/static/index.html` 第 view-hotkw 区
- 模板：`scripts/scrape_tk_hot.py`（廖复制改用）

---

## ✅ 2026-05-09 已完成 — v3.9.0 creator 主表合并廖字段

- migrate_v14：creator 新增 50 列（廖独有的评分/推荐/证据），ETL 一次性 UPSERT
- creator 行数 66 → 132，outreach 101 行 FK 全保留
- `tk_creators` 仍是廖原始落地表，每次 server 启动会跑 v14 重新合并（idempotent，BD 字段保护）
- 邀约话术生成器 build_prompt 携带 8 个廖信号字段 + evidence snippets
- 廖加新列时：对 `tk_creators` ALTER ADD → 把列名追加到 `migrate_v14.py` NEW_COLS

---

## ✅ 2026-05-08 已完成 — v3.8.0 廖 lead 池接收层

- 9 张新表 + 79 tag seed 全部就位（migrate_v11）
- 廖可直接 `POST /api/v1/data/creator_leads/bulk` 灌爬虫数据
- admin（张+廖）能给任何内置表加列（`POST /api/v1/tables/{name}/columns`）
- **前端 "🕷️ Lead 池" tab** 已上线，3 条 demo lead 可见，按 priority 降序，支持 5 维筛选
- **后续要做**：
  - `creators` lead 池 → `creator` 主表的 ETL 脚本（promote 规则待和张定）
  - 删 demo 数据（id=demo_001/002/003）等廖真实数据进来时
  - "→ 推到主表" 行内按钮（lead 详情 → 一键 promote）

---

## 📦 待你/廖/团队补的数据（NULL 字段）

服务**已经在跑**，只是某些字段大量为空，需要慢慢填：

| 字段 | 当前空率 | 谁填 | 怎么填 |
|---|---|---|---|
| `creator.followers` | ~98% (65/66) | 廖爬虫 | `PATCH /api/v1/data/creators/{id}` 或 `POST /bulk` |
| `creator.email` | 100% | 张/团队收集 | UI 编辑 |
| `creator.engagement_rate` | 100% | 廖爬虫 | 同上 |
| `creator.country` | 大部分默认 'US' | 廖爬虫 | 同上 |
| `product.amazon_url` | 100% | 张 | UI 编辑 |
| `product.short_url` | 100% | 张 | UI 编辑 |
| `tk_hot_keyword` 真实数据 | 100% (24 条都是 bootstrap seed) | 张 | 路线 A+B（见上方） |

---

## 📋 后续 backlog（按需求确认表）

需求确认表里**还没动**或**只做了一部分**的：

| 任务编号 | 状态 | 备注 |
|---|---|---|
| 2.1 / 2.2 / 2.3 产品/Listing/店铺基础 | ❌ | 需要 TK Shop 后台对接，目前手动 |
| 3.1.1 达人标签划分 | ⚠ 部分 | category_tags 字段在，但分类靠廖爬虫填 |
| 3.1.2 按粉丝/互动/匹配筛选 | ✅ | 已建 4 条命名查询 + UI 筛选 |
| 3.1.3 中腰部 KOC + 排除竞品 | ✅ | 已建 |
| 3.1.4 达人分级台账 | ✅ | 现有 creator 表 + tier 自动分级 + outreach 流水台账 |
| 3.2.1 邀约话术定制 | ✅ | 后端 + 前端 + outbox 队列 |
| 3.2.2 多渠道触达（邮件/IG/WhatsApp） | ⚠ 部分 | outbox 已建；TT DM 自动发要廖做 Playwright；邮件等收集邮箱后接 SMTP |
| 3.2.3 定时邀约 | ❌ | 等廖做调度器 |
| 3.2.4 24h 跟进 | ❌ | 命名查询 creators_follow_up 已就位，等廖做自动检查 |
| 3.3 合作洽谈 | ❌ | 流程 SOP 化，无 AI 介入 |
| 3.4.4 物流自动跟踪 | ❌ | 廖的工作 |
| 3.5.4 视频曝光跟踪 | ⚠ 部分 | 数据层就位 (outreach.video_views 等)，等廖做爬虫 + outreach_video_tracking 命名查询已建 |
| 4.1.4 素材授权跟踪 | ⚠ 部分 | outreach.ad_auth_code 字段 + auth_pending 命名查询已建 |
| 4.4 广告投放监控 | ❌ | 廖建 ad_campaigns 表 + 投放数据接入（让他用 `POST /api/v1/tables` 自助建） |
| 5.1 数据复盘 | ⚠ 部分 | exports/ 有 xlsx，缺自动周报 |
| 5.2 优质达人复投 | ❌ | 缺 quality_score 模型 |
| 5.3 素材迭代 | ❌ | 缺素材库表 |

---

## 🐞 已知小问题（不阻塞但可优化）

- 前台 SVG 趋势图只在快照>=2 时好看；新关键词只有 1 个点
- AI brainstorm 还没建（路线 B）
- xlsx 导出没自动定时
- audit_log 增长无清理策略，长跑后会越来越大（建议每月归档）

---

## 📝 给新会话的话

如果接到这份的新 Claude：
1. 先读 [项目交接.md](项目交接.md) 拿全局观
2. 再读 [文件地图.md](文件地图.md) 找入口
3. 然后按本文件 §"🔥 当前正在做" 干起 — 第一步是建"+ 手动添加关键词"按钮

不需要看历史对话。所有共识都已写在 [决策日志.md](决策日志.md)。
